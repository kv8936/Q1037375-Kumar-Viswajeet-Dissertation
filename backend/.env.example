# Copy this file to `.env` and edit values.

# URL for the Python inference service
INFERENCE_URL=http://127.0.0.1:8001

# Optional: Postgres database URL for logging (if empty, logging is disabled)
# Example: postgres://user:password@localhost:5432/hazard_chatbot
DATABASE_URL=
